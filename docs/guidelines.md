# Project Guidelines

- **Package Manager:** Always use or suggest `bun`. Never use `npm`.
- **Authentication:** Use our own JWT issued by the backend, rather than relying solely on the Google token.
- **Authentication Library:** Use `@react-oauth/google` for Google Login.
- **Documentation:** Update `README.md` (or relevant docs) whenever behavior or structure changes.
